const express=require('express');const c=require('../controllers/employeeController');module.exports=({auth,admin})=>{const r=express.Router();r.get('/generate-id',auth,admin,c.generateEmployeeId);r.get('/dropdown',auth,c.getEmployeeDropdown);r.get('/',auth,admin,c.getEmployees);r.get('/:id',auth,c.getEmployeeById);r.post('/',auth,admin,c.createEmployee);r.put('/:id',auth,admin,c.updateEmployee);r.patch('/:id/status',auth,admin,c.updateStatus);r.delete('/:id',auth,admin,c.deleteEmployee);return r;};
