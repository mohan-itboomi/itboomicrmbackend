const express=require('express');const c=require('../controllers/authController');module.exports=({auth})=>{const r=express.Router();r.post('/login',c.login);r.post('/refresh',c.refresh);r.post('/refresh-tokens',c.refresh);r.post('/forgot-password',c.forgotPassword);r.post('/reset-password',c.resetPassword);r.get('/me',auth,c.getMe);r.post('/logout',auth,c.logout);r.post('/change-password',auth,c.changePassword);return r;};
