const express=require('express'); const c=require('../controllers/projectController'); const roles=(...allowed)=>(req,res,next)=>allowed.includes(String(req.user?.role||'').toLowerCase())?next():res.status(403).json({success:false,message:'Insufficient permissions'}); module.exports=({auth,admin,projectAccess,projectManager})=>{const r=express.Router();const documentAccess=roles('admin','team-lead','bd','project-coordinator','employee','web-developer','mobile-developer','designer','tester');const documentManager=roles('admin','bd','designer');r.get('/generate-id',auth,projectManager,c.generateProjectId);r.get('/generate-code',auth,projectManager,c.generateProjectId);r.get('/',auth,documentAccess,c.getProjects);r.get('/:id',auth,documentAccess,c.getProjectById);r.post('/',auth,projectManager,c.createProject);r.put('/:id',auth,projectManager,c.updateProject);r.patch('/:id/scope',auth,documentManager,c.updateScope);r.patch('/:id/frd',auth,documentManager,c.updateFrd);r.delete('/:id/frd/:frdId',auth,documentManager,c.deleteFrd);r.post('/:id/phases',auth,projectManager,c.addPhase);r.patch('/:id/phases/:phaseId',auth,projectManager,c.updatePhase);r.delete('/:id/phases/:phaseId',auth,projectManager,c.deletePhase);r.post('/:id/members',auth,projectManager,c.addMember);r.delete('/:id/members/:userId',auth,projectManager,c.removeMember);r.delete('/:id',auth,admin,c.deleteProject);return r;};
